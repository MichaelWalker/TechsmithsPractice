import java.util.*;

public class TrickierThings {

    // Fill in the function so that it returns the number of unique words in the input.
    public static Integer countUniqueWords(List<String> inputWords) {
        return 0;
    }

    // Fill in the function so that it returns a map of each word that appears in the input, to how often it appears.
    // ie if the input is ["happy" "sad" "happy"], then return { "happy": 2, "sad": 1 };
    public static Map<String, Integer> countOccurrencesOfWords(List<String> inputWords) {
        return new HashMap<>();
    }

    // Fill in the function so that it returns the most common starting letter for the input words.
    // ie if the input is ["hi", "there", "Henry"], then return "h", as 2 of the words start with the letter h.
    public static String findTheMostCommonFirstLetter(List<String> inputWords) {
        return null;
    }

    // Fill in the function so that it returns the names of all the dogs belonging to the owner.
    // For example, for the name 'Mike', we might return ["Molly", "Cliff"].
    // the 'ownerDogMap' is a map of the name of the owner to a list of all the dogs they own.
    public static List<String> findTheNameOfTheDogBelongingTo(String ownerName, Map<String, List<Dog>> ownerDogMap) {
        return null;
    }
}
